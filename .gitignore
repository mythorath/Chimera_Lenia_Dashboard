node_modules/
dist/
*.log
.DS_Store

# runtime data (db + recordings) — never commit
server/data/
*.db
*.db-shm
*.db-wal

# env / secrets
.env
.env.local

# GPU dream renderer venv (install via dream/_setup.sh)
dream/.venv/

# local runtime logs
logs/
